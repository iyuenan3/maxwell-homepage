.DS_Store

# maxwellii.com static site — 本地配置（含 SSH 别名 / 远端路径覆盖）
site/.env
site/.env.local

# chat-api (Next.js LLM 后端) — node 工程产物 + 含 secret 的 env
**/node_modules/
**/.next/
**/.env
**/.env.local
**/.env.*.local

# RAG embeddings 和本地运行数据（本地 build，rsync 推送，不入 git）
chat-api/data/

# RAG scan/judge artifacts（本地审稿用，绝对不入 git — 路径暴露 + 摘要）
chat-api/scripts/manifest.json
chat-api/scripts/manifest.json.*.bak
chat-api/scripts/manifest-report.md
chat-api/scripts/migration-report.md

# Claude Code 本地状态（cron lock 等）
.claude/

# Claude Code 本地工作指引（含具体路径 / 内部约定，本地用，不入 git）
CLAUDE.md
